[pytest]
testpaths = tests
pythonpath = scripts
addopts = -v --tb=short --timeout=10
markers =
    pure: Pure function tests (no Document dependency)
    integration: Tests that create/modify docx files
    slow: Tests that involve file I/O or ZIP manipulation
